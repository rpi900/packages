# # Set IPADDRESS to the local network IP address of the Raspberry Pi:
# IPADDRESS=10.1.1.2

# # For a base station, set PROXY to the name of the internet-facing network
# # interface (either eth0 for ethernet or wlan0 for wireless):
# PROXY=eth0

# # Set OPTIONS to any other PPP command-line options you need, e.g.:
# OPTIONS=noauth

# # If using DNT900 radio in transparent mode, set SPEED to the serial rate
# # being used (as determined by the radio's SerialRate register):
# SPEED=9600
